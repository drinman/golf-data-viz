/**
 * Strokes Gained Benchmarker — Tool #1
 *
 * Free web tool where any golfer inputs round stats and sees where they
 * gain/lose strokes vs handicap peers (not PGA Tour pros).
 *
 * Produces a shareable spider diagram or heat map.
 *
 * MVP flow:
 * 1. User enters handicap index
 * 2. User inputs round stats (manual form)
 * 3. Tool calculates strokes gained per category vs peer bracket
 * 4. Displays interactive Nivo radar chart
 * 5. User can export/share as image
 *
 * Future:
 * - Arccos API import
 * - Garmin CSV import
 * - Multi-round trend analysis
 */

export default function StrokesGainedPage() {
  return (
    <div>
      <h1>Strokes Gained Benchmarker</h1>
      <p>Coming soon.</p>
    </div>
  );
}
